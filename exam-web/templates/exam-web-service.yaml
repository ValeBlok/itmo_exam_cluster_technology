apiVersion: v1
kind: Service
metadata:
  name: exam-web-service
  namespace: exam-k8s
spec:
  type: ClusterIP
  selector:
    app: exam-web
  ports:
    - port: {{ Values.service.port }}
      targetPort: {{ Values.service.targetport }}
      protocol: TCP